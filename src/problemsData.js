export const problemsData = [
    {
        id: "1",
        name: "Ideal Point",
        tags: "brute force, implementation, greedy",
        diff: "900",
        solved: "15436",
        problem: {
            key: 1,
            timeLimit: "2 seconds",
            memoryLimit: "256",
            input: "standard input",
            output: "standard output",
            statement:
                "You are given none-dimensional segments (each segment is denoted by two integers — its endpoints).Lets define the function f(x)as the number of segments covering point x(a segment covers the point xif l≤x≤r, where lis the left endpoint and ris the right endpoint of the segment).",
            inputStatement:
                "The first line contains one integer t(1≤t≤1000) — the number of test cases.The first line of each test case contains two integers n and k(1≤n,k≤50).Then n lines follow, i-th line of them contains two integers li and ri(1≤li,ri≤50; li≤ri) — the endpoints of the i-th segment.",
            outputStatement:
                "For each test case, print YES if it is possible to remove some (possibly zero) segments, so that the given point k becomes ideal, otherwise print NO.You may print each letter in any case (YES, yes, Yes will all be recognized as positive answer, NO, no and nO will all be recognized as negative answer)."
        },
    },
    {
        id: "2",
        name: "not Ideal Point",
        tags: "brute force, implementation, greedy",
        diff: "900",
        solved: "15436",
        problem: {
            key: 1,
            timeLimit: "2 seconds",
            memoryLimit: "256",
            input: "standard input",
            output: "standard output",
            statement:
                "You are given none-dimensional segments (each segment is denoted by two integers — its endpoints).Lets define the function f(x)as the number of segments covering point x(a segment covers the point xif l≤x≤r, where lis the left endpoint and ris the right endpoint of the segment).",
            inputStatement:
                "The first line contains one integer t(1≤t≤1000) — the number of test cases.The first line of each test case contains two integers n and k(1≤n,k≤50).Then n lines follow, i-th line of them contains two integers li and ri(1≤li,ri≤50; li≤ri) — the endpoints of the i-th segment.",
            outputStatement:
                "For each test case, print YES if it is possible to remove some (possibly zero) segments, so that the given point k becomes ideal, otherwise print NO.You may print each letter in any case (YES, yes, Yes will all be recognized as positive answer, NO, no and nO will all be recognized as negative answer).",
        },
    },
    {
        id: "3",
        name: "Ideal Point",
        tags: "brute force, implementation, greedy",
        diff: "900",
        solved: "15436",
        problem: {
            key: 1,
            timeLimit: "2 seconds",
            memoryLimit: "256",
            input: "standard input",
            output: "standard output",
            statement:
                "You are given none-dimensional segments (each segment is denoted by two integers — its endpoints).Lets define the function f(x)as the number of segments covering point x(a segment covers the point xif l≤x≤r, where lis the left endpoint and ris the right endpoint of the segment).",
            inputStatement:
                "The first line contains one integer t(1≤t≤1000) — the number of test cases.The first line of each test case contains two integers n and k(1≤n,k≤50).Then n lines follow, i-th line of them contains two integers li and ri(1≤li,ri≤50; li≤ri) — the endpoints of the i-th segment.",
            outputStatement:
                "For each test case, print YES if it is possible to remove some (possibly zero) segments, so that the given point k becomes ideal, otherwise print NO.You may print each letter in any case (YES, yes, Yes will all be recognized as positive answer, NO, no and nO will all be recognized as negative answer).",
        },
    },
    {
        id: "4",
        name: "Ideal Point",
        tags: "brute force, implementation, greedy",
        diff: "900",
        solved: "15436",
        problem: {
            key: 1,
            timeLimit: "2 seconds",
            memoryLimit: "256",
            input: "standard input",
            output: "standard output",
            statement:
                "You are given none-dimensional segments (each segment is denoted by two integers — its endpoints).Lets define the function f(x)as the number of segments covering point x(a segment covers the point xif l≤x≤r, where lis the left endpoint and ris the right endpoint of the segment).",
            inputStatement:
                "The first line contains one integer t(1≤t≤1000) — the number of test cases.The first line of each test case contains two integers n and k(1≤n,k≤50).Then n lines follow, i-th line of them contains two integers li and ri(1≤li,ri≤50; li≤ri) — the endpoints of the i-th segment.",
            outputStatement:
                "For each test case, print YES if it is possible to remove some (possibly zero) segments, so that the given point k becomes ideal, otherwise print NO.You may print each letter in any case (YES, yes, Yes will all be recognized as positive answer, NO, no and nO will all be recognized as negative answer).",
        },
    },
];
